import re

# อ่านไฟล์ shop.html
with open("shop.html", "r", encoding="utf-8") as f:
    html = f.read()

print("--- Start Update Process ---")

# 1. ตัวอย่าง: อัปเดตชื่อร้าน (ถ้าต้องการเปลี่ยนชื่อร้านทุกหน้า)
# html = re.sub(r'<div class="nav-logo">.*?</div>', '<div class="nav-logo">SET<span>SHOP</span> NEW VERSION</div>', html)

# 2. ตัวอย่าง: อัปเดตข้อความประกาศ (Announcement)
new_announcement = '''
            <div style="text-align: center; margin-bottom: 30px;">
                <h2 style="font-size: 20px; color: #fff;">📢 ประกาศ: ร้านอัปเดตระบบใหม่แล้ว!</h2>
                <p style="font-size: 12px; color: #555; margin-top: 4px;">เพิ่มระบบภาพสินค้าถาวรและความปลอดภัยสูงขึ้น</p>
            </div>
'''
# ใช้ Regex เปลี่ยนเฉพาะส่วนประกาศ โดยไม่แตะส่วนสินค้า
html = re.sub(r'<div class="announcement-card">.*?<div class="announcement-grid">', 
              '<div class="announcement-card">' + new_announcement + '<div class="announcement-grid">', 
              html, flags=re.DOTALL)

# --- ส่วนสำคัญ: เราจะไม่ใส่โค้ดที่ไปเปลี่ยน products-grid หรือ defaultProducts แล้ว ---
# เพราะตอนนี้ shop.html จะไปดึงข้อมูล "สด" จาก server.js แทน 
# ทำให้รูปภาพที่คุณใส่ผ่าน Admin อยู่คงทนถาวรครับ!

with open("shop.html", "w", encoding="utf-8") as f:
    f.write(html)

print("✅ อัปเดตงานดีไซน์เรียบร้อย และรักษาข้อมูลสินค้าเดิมไว้ได้ 100%")
